<h2>Welcome to Store</h2>

<div class="products">
  {{#each product in controller}}
    {{view App.ProductItemView contentBinding="product"}}
  {{/each}}
</div>

<div class="cart">
  {{view App.CartView}}
</div>
